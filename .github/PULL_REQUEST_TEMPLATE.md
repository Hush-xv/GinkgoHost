<!-- 描述改了什么、为什么 / What and why -->

## 改动内容 / Changes

-

## 验证方式 / Verification

- [ ] `dotnet build GinkgoHost -c Release` 通过
- [ ] `dotnet run --project GinkgoHost/tests/SmokeTest -c Release` 全部通过
- [ ] 真机验证过涉及的总线路径（如适用）/ verified on real hardware if bus paths touched
